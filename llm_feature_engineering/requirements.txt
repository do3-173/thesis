# Core dependencies
hydra-core>=1.3.0
omegaconf>=2.3.0
pandas>=1.5.0
numpy>=1.21.0
scikit-learn>=1.1.0
matplotlib>=3.5.0
seaborn>=0.11.0

# LLM APIs (for API-based models - optional)
anthropic>=0.7.0
openai>=1.0.0
python-dotenv>=0.19.0

# Local LLM inference (HuggingFace) - for running Qwen-7B on 4090
transformers>=4.40.0
accelerate>=0.27.0
bitsandbytes>=0.43.0  # For 4-bit quantization on 24GB GPUs
sentencepiece>=0.1.99
tokenizers>=0.15.0

# Downstream Models - Required for experiments
lightgbm>=4.0.0

# Traditional AutoML Feature Engineering
featuretools>=1.28.0
autogluon.tabular>=0.8.0  # For AutoGluon FE (Loris requirement)
# auto-sklearn>=0.15.0  # Linux only - install separately, see below

# Utilities
tqdm>=4.65.0

# Development dependencies (optional)
pytest>=7.0.0
black>=22.0.0
flake8>=4.0.0
mypy>=0.950

# Note: PyTorch should be installed separately with CUDA support:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# Optional (heavy dependencies, install separately if needed):
# autogluon.tabular>=0.8.0
# auto-sklearn>=0.15.0  # Linux only