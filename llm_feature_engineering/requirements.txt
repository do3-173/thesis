# Core dependencies
hydra-core>=1.3.0
omegaconf>=2.3.0
pandas>=1.5.0
numpy>=1.21.0
scikit-learn>=1.1.0
matplotlib>=3.5.0
seaborn>=0.11.0

# LLM APIs (for API-based models - optional)
anthropic>=0.7.0
openai>=1.0.0
python-dotenv>=0.19.0

# Local LLM inference (HuggingFace)
transformers>=4.40.0
torch>=2.0.0
accelerate>=0.27.0  # For efficient model loading and device_map

# Downstream Models - Required for experiments
lightgbm>=4.0.0

# AutoGluon (optional - for benchmark)
autogluon.tabular>=0.8.0

# Traditional AutoML Feature Engineering (optional)
featuretools>=1.28.0

# Auto-sklearn (optional - Linux only, requires specific setup)
# auto-sklearn>=0.15.0  # Uncomment on Linux with proper setup

# TALENT benchmark (optional)
# Note: Install manually if needed

# Development dependencies (optional)
pytest>=7.0.0
black>=22.0.0
flake8>=4.0.0
mypy>=0.950